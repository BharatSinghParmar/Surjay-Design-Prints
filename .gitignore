.next
node_modules
out
dist
*.log
.env*
!.env.example

# Local admin design catalogue (dev driver)
.data
public/uploads

# Raw media originals (large) — processed versions go to public/
media-inbox
.DS_Store
**/.DS_Store
tsconfig.tsbuildinfo

# Local editor/agent config (machine-specific paths)
.claude

# Generated video assets — regenerable from scripts/, large binaries
video-banners
videos
